# 配置文件：EmbMQTTNode 节点配置
# 格式 key = value，# 开头为注释

# MQTT 服务器地址
broker_host = 127.0.0.1

# MQTT 服务器端口
broker_port = 1883

# 数据上报主题
topic = embmqttnode/data

# 本节点客户端 ID，建议每台设备唯一
client_id = emb-node-01

# 采样间隔，单位毫秒
sample_interval_ms = 5000

# 传感器类型：mock / bmp280 / sht30
sensor_type = mock
